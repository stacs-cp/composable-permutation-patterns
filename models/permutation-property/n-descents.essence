given length : int
find perm : sequence (size length, injective) of int(1..length)

$ number of descents
given nDescents : int
given turnedOn : bool
such that turnedOn ->
    nDescents =
        sum([ 1
            | i : int(1..length-1)
            , perm(i) > perm(i+1)
            ])