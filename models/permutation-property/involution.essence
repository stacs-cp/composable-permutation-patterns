given length : int
find perm : sequence (size length, injective) of int(1..length)

$ perm is an involution
$ perm * perm = id

given turnedOn : bool
such that turnedOn ->
    forAll i : int(1..length) .
        perm(perm(i)) = i
